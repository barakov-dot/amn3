# Phase 16: минимальный AWG3.1-пилот

Реализация: `scripts/vps/phase16_awg31_minimal_pilot.py`. Локальная подготовка от commit `830517a`. Это отдельный сценарий, не изменение package 016 и не разрешение на серверные операции.

Локальная поправка 2026-08-28: целевой путь профилей изменён на `/var/lib/amn2-phase16/pilot-input`; будущие ключи — `/var/lib/amn2-phase16/pilot-keys`. Read-only проверка установила, что `/var/lib/amn2-spain` принадлежит не root, поэтому использовать его родителем root-only секретов нельзя. Проверки владельца/прав не ослаблены, права существующих каталогов не меняются. Прежний файл на сервере с SHA256 `95f0f52b875328425df54b0564e70bfdcb2bfef4ac2ccac0aeef8fb5c29a2f92` не изменён; обновлённая версия требует отдельного разрешения на новую загрузку, без перезаписи старой.

## Текущее состояние

2026-08-28, 22:00 МСК: после повторного подключения оператор снова сообщил об отсутствии интернета. Серверная read-only проверка подтвердила настоящий handshake AWG3.1 и двусторонний трафик: RX 386172, TX 1165377 байт. Firewall пилота полный, прежние правила не изменились, UDP-ошибок нет. Это подтверждает работу протокола, но не клиентскую acceptance. VPN на АРМ затем отключён; требуется короткая локальная проверка маршрута, DNS и HTTPS при следующем включении. Task 4.5 остаётся незакрытым.

2026-08-28, 21:49 МСК: по разрешению оператора добавлены временные host firewall/NAT-правила только для существующего пилота UDP 30002. Штатный `nft --check` прошёл; проверены исходящий TCP из netns пилота и входящий UDP с АРМ до сокета контейнера. AWG2 и прежние firewall-правила сохранены. Конфиг, ключи, контейнер и package 016 не менялись. Реального VPN-handshake пока нет: оператору предложен повторный тест того же профиля, без переимпорта. Правила не сохраняются для перезагрузки или reload firewall. Подробности: research/amn2/phase16-pilot-firewall-fix-2026-08-28.md.

Предыдущий неуспешный клиентский тест:

2026-08-28, 21:20 МСК: первый тест на АРМ не прошёл. Handshake отсутствует, UDP-счётчики пилотного контейнера нулевые. Выявлен пробел подготовки firewall/NAT хоста для нового пилота. В этом диагностическом ходе настройки сервера и клиента не менялись; Task 4.5 и acceptance заблокированы. Подробности: research/amn2/phase16-first-arm-test-diagnosis-2026-08-28.md.

Предыдущая готовность к тесту:

2026-08-28, 14:16 МСК: изолированный AWG3.1 запущен, штатная проверка server/client PASS, один peer, UDP 30002. Отпечатки AWG2 до и после запуска совпали. Windows-конфиг передан на АРМ в защищённый каталог вне Git; версия клиента 5.0.1.5 проверена. Импорт оператором и реальный трафик ещё не подтверждены.

Текущий скрипт — v4, SHA256 3700284de8ac226a958b0fc4f1d88d93763987698ee080029858b872f9789649. Входы — /var/lib/amn2-phase16/pilot-input-v2. Из прежней пары удалён только AdvancedSecurity = on: официальный userspace backend awg-tools отвергает этот kernel-only флаг. HeaderProtectionKey и остальные параметры AWG3.1 сохранены, ключи прежние, старые версии файлов не перезаписаны. Один RED, затем целевой набор 22 PASS.

Рабочая попытка — pilot-spain-awg31-arm-20260828-002. Первая попытка сохранила STOP и квитанцию отката. Подробности: research/amn2/phase16-minimal-pilot-live-start-and-arm-delivery-2026-08-28.md.

## Режимы и история подготовки

Фактическая подготовка v2 (2026-08-28): закреплённый образ загружен, штатные ключи и `server.conf`/`windows.conf` созданы в root-only namespace Phase 16. Runtime не запускался, профили оператору не выдавались. Первичный итоговый контроль сохранил STOP из-за различия AWG2 fingerprints. Последующее read-only сравнение обнаружило изменение только порядка тех же семи peer при совпадении остальных компонентов двух диагностических снимков. Локально добавлена сортировка строк перед хешированием; это не изменение AWG2. Один целевой RED/GREEN тест подтвердил нечувствительность к порядку и сохранение обнаружения смены/дублирования peer; полный набор не повторялся. Подробности: `research/amn2/phase16-minimal-pilot-v2-preparation-receipt-2026-08-28.md`.

Последующая загрузка v3 (2026-08-28, 13:33 МСК): по отдельному разрешению исправленный скрипт загружен в новый `/var/lib/amn2-phase16/pilot-tools/v3/`; один `check --with-profiles` завершился PASS. Хеши существующей пары совпали с подготовкой v2, ключи и профили не генерировались заново. SHA256 свежего состояния: `caebba5ff4f3d830976a73817d22197d5d05b07300071a4e117ad1bcaae2cc03`. Старый STOP не переписан; новый результат относится к новой проверке. Runtime и выдача конфига на АРМ всё ещё требуют отдельных разрешений. Подробности: `research/amn2/phase16-minimal-pilot-v3-upload-check-receipt-2026-08-28.md`.

- Без аргументов / `plan`: только несекретный JSON-план, без внешних команд и записи файлов.
- `check`: read-only проверка Ubuntu 24.04, amd64, root, dedicated Docker, TUN, forwarding, адреса сервера, свободных ресурсов и снимка AWG2. Ключи и конфиги не требуются.
- `render --key-directory PATH --dns ADDRESS --mtu 1280`: создание ровно двух защищённых файлов — серверного и одного Windows-клиентского. Требует отдельного разрешения; работает только на Linux.
- `check --with-profiles`: дополнительно проверяет пару конфигов и возвращает их хеши.
- `apply`: требует `--script-sha256`, `--state-sha256`, `--server-sha256`, `--client-sha256`, `--claim`. Повторно проверяет состояние, сохраняет одноразовую запись попытки и защищённый снимок входных файлов, проверяет конфиги штатным AWG и запускает один runtime.

Ключи для `render` должны быть заранее созданы штатными `awg genkey`, `awg pubkey`, `awg genpsk` в отдельно разрешённом шаге. Имена защищённых входных файлов: `server_private.key`, `server_public.key`, `client_private.key`, `client_public.key`, `psk.key`, `hpk.key`. Секреты не передаются в аргументах, не печатаются и не включаются в Git. `render` не генерирует ключи и не перезаписывает существующий каталог.

## Изоляция и ограничения

Используется закреплённый образ `amneziavpn/amneziawg-go` с digest `4e1fd2840f8d26eb6ec8bc1598e66f2f17f5d0201cd2baadbde560c104d4fc9d`. Контейнер `amn2-spain-awg31-pilot`, сеть `amn2sp31pilot`, UDP 30002, VPN `10.212.13.0/24`; прежние приложение, SQLite, Telegram и SSH-bootstrap не используются.

Проверка реальным `awg`/`awg-quick` и сверка private/public выполняются в контейнере `--network none`, до создания постоянной сети и публикации порта. Runtime явно userspace; клиентский приватный ключ в рабочий контейнер не монтируется. Сравнение AWG2 использует наблюдавшиеся `amn2-spain-awg` / `awgsp0`, без требования клиентского трафика. Старый freshness-контракт package 016 не изменён.

На ошибке удаляются только ресурсы данной попытки, по ID или уникальной метке; неуспешный откат помечается `failed_or_unknown`. Образ остаётся в кэше; входные конфиги, их защищённый снимок и журнал сохраняются. Это не полный возврат файловой системы к прежнему состоянию. Нет автоматического повтора, удаления AWG2 или общей выдачи.

Это IPv4-пилот. `::/0` направляется клиентом в туннель, но сервер IPv6 не маршрутизирует; отсутствие утечек требует клиентского теста. MTU 1280 — начальная настройка пилота, не диагноз медленного AWG2. Автоматический перезапуск отключён. Успех запуска означает только `pilot_started_client_test_pending`, не acceptance.

Временное сетевое дополнение: `scripts/vps/phase16_awg31_pilot_firewall.py` формирует одну собственную NAT-таблицу `ip amn2_p16_awg31` и три адресно-интерфейсных правила в существующем `ip filter FORWARD`. Оно не очищает ruleset, не меняет политики цепочек и не отключает UFW. Перед применением обязательны native-check, сверка состояния и bindings существующего пилота; при ошибке проверки удаляются только добавленные ресурсы. Модуль сам по себе не запускает SSH. Будущая постоянная интеграция и автоматический запуск остаются отдельной задачей после успешного теста.

Первоначальная подготовка требовала отдельных разрешений на передачу файла, подготовку секретов и применение одного профиля; фактически полученные разрешения и результаты перечислены выше. Старые package approval/transaction не переиспользуются. Текущее разрешение на достижение первого теста не распространяется на общую выдачу, изменение AWG2 или публичную публикацию истории. `check` не заменяет проверку firewall-пути и клиентской версии; скрипт проверяет bindings, но сам не выдаёт операторское разрешение.

## Локальная проверка 2026-08-28

TDD: ожидаемый RED отсутствующего исполнителя. Первый набор: 16 PASS, две ошибки одной опечатки `readstring`; после исправления одной строки — 18 PASS за 0.319 s. Затем отдельный RED/GREEN одного теста разделения `check` / `check --with-profiles`: PASS за 0.015 s, без повторения полного набора. Итого покрыты 19 тестовых методов; проверки Docker/хоста заменены локальными имитациями, используются только синтетические ключи. Проверен синтаксис трёх shell-фрагментов Git Bash и инертный CLI реальным Python-процессом.

WSL/Docker на рабочем компьютере недоступны. Реальный Linux runtime, штатный parser внутри образа и трафик ещё не проверены. В этом шаге SSH, Spain egress, stage/install, настоящие ключи/конфиги, materialization и package verifier не запускались. Package 016 и AWG2 не изменялись; push не выполнялся.

Последующая поправка пути: один новый тест `test_pilot_inputs_use_root_owned_phase16_namespace` — RED на старом пути (0.045 s), GREEN после изменения одной константы (0.008 s). Тест также сохраняет отказ для non-root родителя. Полный набор не повторялся. Серверная подготовка остановилась до image pull/keygen/render; её фактический результат и отдельное read-only уточнение записаны в `research/amn2/phase16-minimal-pilot-keygen-render-stop-2026-08-28.md`.

Источники: [официальный runtime и параметры](https://github.com/amnezia-vpn/amneziawg-go), [официальный parser](https://github.com/amnezia-vpn/amneziawg-tools/blob/master/src/config.c), [awg-quick](https://github.com/amnezia-vpn/amneziawg-tools/blob/master/src/wg-quick/linux.bash).
