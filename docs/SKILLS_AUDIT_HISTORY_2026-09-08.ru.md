# Исторический снимок — SKILLS_MAP.ru.md

Зафиксирован 2026-09-08 из source HEAD fedcbf347511de2e1a229b6f9615da61ca5d6b90.
Это сохранённое доказательство, не текущий execution plan или разрешение.
Старые статусы, следующие шаги и GO действуют только в историческом контексте.
Актуальный документ: [SKILLS_MAP.ru.md](SKILLS_MAP.ru.md). Читать историю только адресно.
SHA256 исходного текста UTF-8 с LF: 7CBB2521C74EF36B130BA1E521B1BF358562D985EF093B68D0AD28ADBE22BDF3.

<!-- BEGIN_PRESERVED_SOURCE -->
# Карта skills и аудит применимости

Дата текущего решения: 2026-09-08.
Статус: `PROJECT_DOCS_AND_PERSONAL_EXPLICIT_ONLY_APPLIED`.
Оператор согласовал пункты 1–2: проектную документацию и invocation metadata
двух личных skills. Это отдельное разрешение, не расширение scope обычных
задач AMN3. Другие проекты, память, SKILL.md и Superpowers не меняются.

## Применённая оптимизация

- Текущие gates вынесены в начало [плана Phase 16](superpowers/plans/2026-08-24-amn2-phase16-awg3-family-3-1-spain-pilot.md);
  [acceptance](PHASE16_ACCEPTANCE_CRITERIA_DRAFT.ru.md) отделён от истории
  реализации методики. Пороги, receipts и прежние ссылки сохранены.
- [Карта кода](CODE_MAP.ru.md) различает HTTP/ICMP side effects и чистую DNS-модель.
  Новые агенты/AGENTS/дублирующие skills не создавались; проектный AGENTS сохранён.
- В `C:/Users/SooL/.codex/skills/sdp-firebird/agents/openai.yaml`
  `policy.allow_implicit_invocation` изменено с `true` на `false`.
- В `C:/Users/SooL/.codex/skills/winui-app/agents/openai.yaml` добавлена
  та же policy со значением `false`. Остальные поля и файлы обеих папок сохранены.
- По [официальному контракту Skills](https://learn.chatgpt.com/docs/build-skills)
  явные вызовы `$sdp-firebird` / `$winui-app` остаются доступны.
  Обнаружение после перечитывания каталога и экономия времени/токенов пока
  НЕ подтверждены; текущая задача может сохранять ранее загруженный каталог.

Откат metadata: вернуть `true` для SDP; удалить добавленный блок `policy`
у WinUI. Не переписывать UI/default_prompt и не удалять skills.
Настройки личные, влияют не только на AMN3; в Git — описание решения,
не копии личных skills. Отсутствие PyYAML в bundled Python заблокировало
`quick_validate.py`; это ограничение валидатора, не доказанный дефект skill.
Ничего не устанавливалось. Exact metadata readback PASS для обоих файлов;
по SHA256 изменились только два `agents/openai.yaml` в обеих папках.
Проверены 71 локальная ссылка/якорь в четырёх затронутых документах,
diff/whitespace и отсутствие secret markers в добавлениях. Прежние тексты
плана/receipts/аудита сохранены по сравнению с HEAD; пороги acceptance неизменны.
Это статическая проверка, не полноценный YAML-validator или runtime discovery.

## Следующий этап: Superpowers — предложение, НЕ применено

1. Read-only проверить штатный обратимый способ отключения/восстановления
   в установленном Codex, зависимости и область действия. Кэш не редактировать.
2. Показать exact target/setting, before-state и rollback оператору; получить
   отдельное согласие на ограниченный пилот без Superpowers. Согласие на два
   личных skills само по себе не отключает плагин.
3. После reload — одна обычная локальная docs-only задача: проверить отсутствие
   ненужных повторных согласований/прогонов и сохранность safeguards в AGENTS.
   Не обещать ускорение до измерения; при ухудшении восстановить before-state.

Кандидат — пилот отключения пакета целиком через подтверждённый штатный механизм.
Не редактировать vendor skills, не удалять пакет, не создавать собственный дубль
Superpowers и не ослаблять live/push approvals или TDD. Изменение может затронуть
другие задачи, поэтому не выполняется попутно.

Read-only подготовка 2026-09-08: локальный manifest — Superpowers 6.3.0;
в `C:/Users/SooL/.codex/config.toml` секция
`[plugins."superpowers@openai-curated"]`, `enabled = true`.
Каталог плагинов подтвердил installed/user-enabled; объявленных app dependencies
и unresolved apps нет. Это не доказывает отсутствие внутренних workflow-связей.
В установленном CLI `plugin --help` есть add/list/marketplace/remove, но нет
отдельного enable/disable. `remove` удаляет кэш и для обратимого пилота не выбран.
[Официальный config reference](https://learn.chatgpt.com/docs/config-file/config-reference)
описывает plugin MCP server enabled, но не устанавливает здесь контракт
переключения всего пакета. Наличие локального `enabled` само по себе не доказывает
способ reload/sync desktop state. Перед пилотом нужен штатный переключатель
в карточке плагина или подтверждение соответствующего контракта. Настройки и
plugin cache не менялись; готовое разрешение на отключение пока не предлагается.

Справочная [карта применимости](#карта-применимости-к-amn3amn2) сохранена ниже.

<details>
<summary>Исторический аудит 2026-09-07 — прежний scope, не текущие команды</summary>

Дата: 2026-09-07. Статус: `STATIC_AUDIT_COMPLETE_OPTIMIZATION_NOT_APPLIED`.
Область: только текущий AMN3/VPS-OPS-LAB и применимость доступных skills к его задачам.
Основание: просьба оператора проверить перегрузку личных skills и приступить к анализу.
Уточнение оператора: в остальные проекты не заходить. Глобальная оптимизация
личных skills, системных инструкций и плагинов исключена из текущего задания.

Это справочник и предложения, не новый SKILL.md, не AGENTS.md и не override
инструкций среды. Он не устанавливает, не отключает и не разрешает запуск skills.
Поведенческая эффективность и экономия времени/токенов не измерялись.

Проектные правила теперь вынесены в [AGENTS.md](../AGENTS.md), навигация —
в [START_HERE](START_HERE.ru.md). Глобальные skills по-прежнему не изменены.

## Что действительно загружается

По официальной документации сначала доступны имя и описание skill; полный
SKILL.md читается при выборе, дополнительные материалы — по необходимости.
Количество установленных skills и суммарный размер их справочников сами по себе
не равны размеру контекста конкретной задачи.

- Личные директории вне `.system` в `$CODEX_HOME/skills`: `sdp-firebird`, `winui-app`.
  Это место хранения, не доказательство авторства или ручной установки.
- Системные skills находятся в `.system`; плагины — в управляемом кэше плагинов.
  Кэш не следует редактировать как собственный исходник: изменение непереносимо
  между версиями и может быть потеряно при обновлении.
- `vpn-evening-monitoring` найден в памяти, а не в активном каталоге skills этой
  задачи. Автоматическое обнаружение этого файла не подтверждено.
- Личный `$CODEX_HOME/AGENTS.md` пуст; `AGENTS.override.md` и `~/.agents/skills`
  при проверке отсутствовали. Перегрузка именно этих файлов не обнаружена.

## Статические находки и адресные предложения

Размеры ниже — весь SKILL.md, включая примеры и разметку; «слова» посчитаны
разделением по пробелам. Это не токены и не ограничение допустимого размера.

| Источник | Размер | Наблюдение | Предложение |
| --- | --- | --- | --- |
| personal `sdp-firebird` | 51 строка / 350 слов | Компактный вход с условной загрузкой references; тематика SDP/Firebird | Не относится к текущей документации и БД AMN2. Не изменять в рамках этого проекта |
| personal `winui-app` | 94 строки / 1572 слова | Большой вход с setup/UI/launch-процедурами; тематика разработки WinUI | Диагностика Windows VPN не является разработкой WinUI. Не изменять в рамках этого проекта |
| plugin `using-superpowers` 6.3.0 | 63 строки / 485 слов | Вход на каждый разговор и требование применения при 1% вероятности; это текстовый механизм лишних активаций | В проектных правилах явно описать scope задач и исключения из избыточного процесса; глобальный skill и vendor-cache не менять |
| plugin `brainstorming` 6.3.0 | 250 строк / 2324 слова | Предусмотрены разные масштабы работы, но каждый заканчивается новым approval; неопределённость ведёт к более тяжёлому процессу | Согласованные локальные задачи не должны повторно проектироваться только ради ритуала. Разделить отсутствующий выбор/новый риск и уже полученное согласие |
| plugin `writing-skills` 6.3.0 | 679 строк / 3779 слов | Pressure scenarios, минимум 5 повторов для вариантов behavior-shaping текста, обязательная deployment-последовательность. Системный `skill-creator` допускает соразмерную проверку | Зафиксировать конфликт процедур до редактирования. Не заявлять skill проверенным по одной статической валидации; метод поведения и стоимость согласовать для конкретной правки |
| memory `vpn-evening-monitoring` | 50 строк / 486 слов | Старый абсолютный путь automation memory не найден. Baseline существует в исходном checkout, но отсутствует в этом worktree | Не переносить автоматически в активные skills. Сначала разрешить актуальные automation ID, рабочую папку и baseline. Память в этом аудите не изменялась |

Выявлены риски по тексту, а не доказанная причина прежних задержек. Например,
длинный специализированный skill не мешает VPN-задаче, пока не выбран ошибочно.
Для двух личных skills проверено существование 21 уникального пути references/
scripts, извлечённого из их SKILL.md; это не аудит содержимого всех зависимостей.
Скрипты skills не запускались, WinGet/setup не выполнялись.

## Карта применимости к AMN3/AMN2

Это справочная маршрутизация для проектного AGENTS.md, а не замена текущих
обязательных инструкций. Наличие в карте не означает чтение всех skills заранее.

| Тип задачи | Подходящие навыки | Граница |
| --- | --- | --- |
| Статус, receipt, правка Markdown | Локальная проверка фактов, ссылок и diff; verification-before-completion при утверждении результата | Не перезапускать pytest, package verifier или VPS-проверки ради docs-only изменения |
| Новый дефект нашего кода | systematic-debugging; test-driven-development при разрешённом fix; проверка результата | Diagnosis не разрешает реализацию. Новый тест отвечает на конкретную гипотезу |
| Новое поведение/интеграция | brainstorming / writing-plans по масштабу и незакрытым решениям | Учитывать уже утверждённый план, не начинать Phase заново |
| Security review | Подходящий codex-security workflow по запрошенному виду аудита | Не запускать глубокий security scan на каждую правку документа |
| Skills / настройки Codex | openai-docs; skill-creator / writing-skills при реальном изменении skills | Глобальная правка влияет на другие проекты; статический аудит не является deployment |
| Windows VPN connectivity | Официальные клиентские evidence и bounded diagnostic plan | Само слово Windows не делает задачу разработкой WinUI; `winui-app` здесь не подходит |
| Схема БД AMN2 | Актуальный schema/migrations в production-репозитории | Не применять `sdp-firebird` без реального SDP/Firebird контекста |
| Мониторинг VPN-релизов | Уточнённый существующий monitoring workflow | Не восстанавливать устаревшие пути/автоматизации по памяти и не менять baseline без разрешения |

## Порядок дальнейшей оптимизации только в проекте

1. Проектный [AGENTS.md](../AGENTS.md) подготовлен: источник текущего статуса,
   scope, границы действий, выбор проверок и ссылка на эту карту. Тексты skills
   и изменчивый машинный каталог не скопированы в инструкции.
2. Для docs-only задач оставить проверку фактов, ссылок, diff и секретов; для
   исправлений кода — конкретный RED/GREEN и релевантный итоговый набор. Не менять
   личные WinUI/Firebird skills ради того, чтобы они не срабатывали в чуждом контексте.
3. Если процессный skill предлагает лишние действия, зафиксировать точное правило
   и его конфликт с согласованной задачей; уточнять проектные инструкции, сохраняя
   ограничения среды. Плагины не редактировать, не переустанавливать и не отключать.
4. Проверка проектного AGENTS.md: понятен ли вход в Phase 16 без старых GO;
   не требует ли docs-only правка runtime-тестов; сохраняются ли TDD для code fix,
   approvals для live/push и отложенный статус iPhone/A/B. Это план проверки,
   не доказательство изменения поведения агента.

Safety-инварианты сохраняются: approvals на live/external writes, отсутствие
секретов в Git/логах, AWG2_UNTOUCHED, package016 immutable, один peer последовательно,
точный push SHA/origin/ref. Skill не может расширить пользовательское разрешение
или отменить ограничения среды. Пока остаются только рекомендации, не отключения.

## Источники и проверка этого изменения

- Локально прочитаны SKILL.md обеих личных skills, `skill-creator`, `openai-docs`,
  `using-superpowers`, `brainstorming`, `writing-skills`, TDD, verification,
  memory `vpn-evening-monitoring`; дополнительно SDP safety и выдержки WinUI audit reference.
  Остальные process SKILL.md измерены, но не объявляются полноценно аудированными.
  Эти чтения касались инструкций skills, не кода/данных других проектов;
  после уточнения оператора глобальные предложения сняты с текущего scope.
- [Official: AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md).
- [Official: Skills](https://learn.chatgpt.com/docs/build-skills).
- Локальный scope: эта карта и ссылка из README; historical receipts, production
  code, skills, plugin cache, memory, настройки и конфиги не меняются.
- Проверки: readback/diff, локальные ссылки, отсутствие secret markers. Runtime,
  skill pressure tests, SSH, stage/install и push не выполняются.

Обновлять карту после изменения применимых skills/их триггеров или выявленного
конфликта. Не повторять полный аудит на каждом ходе.

</details>
