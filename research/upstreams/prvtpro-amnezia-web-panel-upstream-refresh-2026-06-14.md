# PRVTPRO/Amnezia-Web-Panel upstream refresh 2026-06-14

Дата: 2026-06-14.

Источник: `PRVTPRO/Amnezia-Web-Panel`.

Automation source: `prvtpro-weekly-upstream-refresh`, heartbeat output visible
in the AMN2 Phase 6 working thread.

License boundary: GPL-3.0 research-only. AMN2 may use only ideas, issue/commit
signals and links. Do not copy upstream code, templates, UI, manager
implementations, scripts or workflow code.

## Observed upstream state

- Latest pushed commit reported by the automation: `fbe5a2b`, dated
  2026-06-08.
- Notable signal: a Telemt path fix was merged upstream, showing that package
  upload/apply flows can fail on missing or drifted asset paths.
- Notable product signals: multiple AmneziaWG instances per server,
  endpoint/DNS/subnet/IPv6 requests, per-user statistics and speed-limit
  requests.

## AMN2 candidates

- `FI-M004`: package asset path preflight. Priority: important. Gate:
  `package/preflight only`. Before any future installer/package apply, verify
  that required operator-kit assets and referenced paths exist, are packaged,
  and are not generated by brittle shell escaping.
- `P6-M005`: multi-instance, port and IPAM conflict model. Priority:
  important. Gate: `local-only/docs/tests`. Record protocol/runtime instances
  as capabilities with port/subnet/address constraints before any live
  multi-server or multi-protocol action.
- Endpoint/DNS/subnet/IPv6 ideas are useful only as future dry-run config model
  and client compatibility tests. They do not open config delivery.

## Gated or deferred

- Per-user statistics and speed limits remain privacy-sensitive and require a
  privacy gate plus write/config/public gate review before product work.
- Multi-instance live actions require live VPS gate, write API/config decisions
  and conflict/rollback design.
- Public/self-service surfaces remain behind `P6-C001` and `P6-C002`.

## Negative controls

- Do not copy GPL code, templates, UI, scripts, workflows or manager
  implementations.
- Do not adopt an admin-equivalent Bearer token model for every route.
- Do not open public exposure, config delivery, write API, Local Agent
  mutations, backup/import/reboot or destructive VPS work from this refresh.

## Для цепочки

- Add `FI-M004` package asset path preflight as an installer/package hygiene
  candidate.
- Treat multi-instance/IPAM as a future local capability model, not a live
  action.
- Keep per-user stats/speed limits behind privacy and write/config gates.
- Keep endpoint/DNS/subnet/IPv6 as dry-run config compatibility work only.
