# Проверка имени при импорте — три клиента

Подготовлено 2026-09-09; обновлено 2026-09-20. Статус: PARTIALLY_EXECUTED.
Это проверочный лист к [исследованию](phase16-client-import-naming-research-2026-09-08.md),
не новый execution plan и не разрешение установки, подключения или выдачи.
Export source: AMN2 b3ed202ba118adb36c474eab5722065ea420d274.

## Условия начала (Windows baseline; отдельный iPhone scope ниже)

Использовать отдельно подготовленную среду без рабочих VPN-профилей, с отключённой
сетью и без автоматического подключения. Изоляция должна быть подтверждена до
импорта; отдельное окно приложения не означает отдельное хранилище профилей.
Не импортировать в существующее рабочее хранилище и не менять его настройки.
Фиксировать ОС, версию и официальный источник конкретной сборки. Для DefaultVPN
соответствие dev-исходника установленной сборке пока не установлено.

Только синтетическая fixture: синтаксически корректные тестовые ключи, никогда
не используемые на сервере, endpoint из документационного диапазона и явно
заданные DNS, MTU, AllowedIPs, keepalive и поддерживаемые AWG-поля. Старые fixtures
с текстовыми placeholders могут быть отвергнуты клиентом до проверки имени;
такой отказ не доказывает дефект envelope. Реальные ключи и профили не нужны.
Синтетическая fixture материализована и использована в Windows Sandbox; см. результат ниже.

## Матрица и последовательность

| Клиент | Вход | Ожидаемое имя | Отдельная проверка |
| --- | --- | --- | --- |
| AmneziaVPN | Родной Amnezia envelope через поддерживаемый импорт файла/ссылки | Neobyatnaya.NET | description, сохранение всех параметров |
| AmneziaWG | Neobyatnaya.NET.conf | Neobyatnaya.NET | basename, точка, длина 15; каждая ОС отдельно |
| DefaultVPN | Родной Amnezia envelope через доступный в сборке импорт | Neobyatnaya.NET | MTU: dev-исходник подставляет default; не считать имя доказательством совместимости |

1. Зафиксировать пустое тестовое хранилище и версии. Если изоляция не доказана — STOP.
2. Импортировать один синтетический артефакт без подключения. Записать фактический
   заголовок; не переименовывать вручную ради PASS. Подпись протокола оценивается отдельно.
3. Сравнить доступные сохранённые/экспортированные параметры с исходной fixture:
   endpoint, адреса, DNS, MTU, AllowedIPs, keepalive, ключевые поля и все AWG-поля.
   Секретные значения в отчёт не включать: достаточно equality/difference/unknown.
   Невидимое поле без поддерживаемого readback получает UNKNOWN, не PASS.
4. Повторить импорт в той же одноразовой среде. Зафиксировать отказ, новый объект,
   суффикс или перезапись. Не подтверждать перезапись. Это отдельная проверка
   поведения приложения, а не доказательство работы existing_names экспортера.
5. Закончить без подключения. Очистка допускается только для точно идентифицированных
   синтетических объектов в одноразовой среде; рабочие профили не затрагивать.

## Критерии и результат

Для каждой ОС/версии независимо записать: input format; import result;
exact_name PASS/FAIL; parameter_preservation PASS/FAIL/UNKNOWN;
duplicate_behavior; connected=false; evidence reference.

Имя PASS только при точном Neobyatnaya.NET после первого импорта без ручной правки.
Изменение MTU или другого параметра означает FAIL сохранности при возможном PASS имени.
Недоступная сборка/UI/изоляция означает NOT_RUN; неподтверждённое поле — UNKNOWN.
Даже полный PASS этого листа не доказывает connectivity или quality и не включает
новый формат в delivery handlers. Решение об интеграции принимается после результатов.

## История блокера и upstream readback — 2026-09-09

2026-09-09 штатный Windows UI runtime не инициализировался:
`windows sandbox failed: helper_unknown_error: apply deny-read ACLs`.
Приложение не запускалось, импорт и подключение не выполнялись. Это ошибка
инструмента, не установленный дефект клиента; обход через рабочие профили не выполнялся.

GitHub API на дату проверки: #3043 открыт, 14 комментариев, последний —
[комментарий оператора](https://github.com/amnezia-vpn/amnezia-client/issues/3043#issuecomment-5584455544).
Более новых комментариев нет. Новые сообщения не отправлялись.

AWG2_UNTOUCHED; package016 immutable; general issuance disabled; stage/install отсутствуют.

## Результат 2026-09-11

AmneziaVPN local 5.0.1.5 + PR #3113: exact_name PASS после импорта и полного
закрытия/открытия; metadata MTU=1280 в preview PASS. Полный parameter_preservation
UNKNOWN, сохранённый MTU после перезапуска UNKNOWN, duplicate_behavior NOT_RUN.
AmneziaWG и DefaultVPN NOT_RUN. Offline-прогон не проверяет туннель.
Подробности и evidence: [receipt](phase16-amneziavpn-pr3113-windows-build-2026-09-09.md#ручная-проверка-windows-sandbox--2026-09-11).

## Результат 2026-09-12

[Итоговый receipt Windows](phase16-windows-import-acceptance-2026-09-12.md):
AmneziaVPN сохранённый MTU1280 и 45 expected fields в двух профилях PASS; duplicate
создаётся. AmneziaWG name/MTU restart PASS, параметры проверены в UI, duplicate
отклонён; keepalive=0 omission объяснён source trace. DefaultVPN/iOS NOT_RUN.
Результат выше за 2026-09-11 исторический. Connectivity/quality не проверены.
## DefaultVPN: подтверждённое и вечерний тест — 2026-09-20

Оператор отдельно разрешил ручной синтетический тест в установленном DefaultVPN
на iPhone с существующими рабочими профилями. Это ограниченное уточнение условий
изоляции выше: рабочие профили, включая «Испанию», не удалять/не менять и не
экспортировать их ключи. General issuance и реальный tunnel test не разрешены.

2026-09-16 перед импортом оператор подтвердил отсутствие Neobyatnaya.NET.
После вставки ранее проверенного synthetic vpn:// скриншот
photo_5282888978031910199_y.jpg подтвердил точное имя Neobyatnaya.NET и
отключённое состояние. NAME_PASS относится только к paste/native пути на этом
неидентифицированном build. На 16.09 перезапуск, параметры и .conf ещё не были подтверждены.
Оператор сообщил, что рабочую «Испанию» импортировал как .conf; это отдельный путь.

### Подготовленные материалы

Старого Temp-файла 20.09 больше нет; причина удаления не установлена.
Из ранее проверенного синтетического vpn:// восстановлен исходный raw .conf
в отдельный каталог вне Git:
C:/Users/SooL/Downloads/Neobyatnaya-import-test-20260920/Neobyatnaya.NET.conf
417 bytes; SHA256 bddc397feaf7346120777946d0339ea91a00fb339f5996b9a656d34ed55837bc.
qCompress length/decompression, description, doc-range endpoint/адреса, DNS,
MTU1280, AllowedIPs=192.0.2.1/32 и keepalive0 проверены; readback bytes совпал.
Рабочие конфиги/ключи не читались; новая реальная конфигурация не выдавалась.

### Короткая последовательность вечером

1. Зафиксировать версию DefaultVPN и iOS, если доступны в «О приложении»/системных
   сведениях. Не обновлять/переустанавливать приложение ради теста. Если версия
   недоступна — UNKNOWN, не подставлять последнюю App Store-версию.
2. Сохранить вид списка профилей до добавления: уже существует synthetic
   Neobyatnaya.NET от paste. Повторять paste-тест или удалять профиль не требуется.
3. Один раз импортировать именно Neobyatnaya.NET.conf через кнопку импорта файла.
   На запрос замены/слияния остановиться, не подтверждать. Записать результат:
   отказ, новое имя, одноимённый дубль или запрос замены. Отказ из-за коллизии
   не объявлять дефектом формата. Без переименования ради NAME_PASS.
4. Если добавился однозначно различимый synthetic профиль — посмотреть доступные
   параметры и сравнить endpoint192.0.2.1:51820, address192.0.2.2/32, DNS
   192.0.2.53/198.51.100.53, MTU1280, AllowedIPs192.0.2.1/32, keepalive0,
   Jc4/Jmin40/Jmax70, S1=16/S2=32, H1=101/H2=202/H3=303/H4=404.
   Нет штатного просмотра — parameter preservation UNKNOWN; не делать backup
   всех рабочих профилей и не просить raw keys ради этой проверки.
5. Полностью закрыть/открыть приложение и проверить имя добавленного synthetic
   профиля, если импорт завершился. Connect не нажимать. Если новый объект
   неотличим от прежних — остановиться до идентификации, ничего не удалять.

Достаточный итог: exact app/version evidence, результат .conf импорта/коллизии,
фактическое имя и только доступные параметры. Если .conf получает «Сервер N»,
зафиксировать NAME_FAIL этого пути; native NAME_PASS остаётся отдельным результатом.
Не требовать одинакового поведения двух путей без доказательства источниками.

### Результат .conf — скриншот получен 2026-09-20

После инструкции импортировать подготовленный Neobyatnaya.NET.conf оператор
прислал C:/Users/SooL/Downloads/Без имени.jpg. SHA256 локального изображения:
73bba00d2cf18fd050a159fce87cccd1f045623fa422204696969cc901118d94.
Изображение и конфиг не добавлены в Git; здесь только нормализованные наблюдения.

- Выбранное имя на экране: **Server 1**, ожидаемое: **Neobyatnaya.NET**.
  Итог проверки имени в контексте этого .conf импорта: **NAME_FAIL**.
- UI одновременно показывает «Не в сети» и **Reconnecting…**. Это наблюдаемая
  попытка подключения, не успешный tunnel/connectivity test. Не установлено,
  запустилась ли она после нажатия кнопки или автоматически при импорте.
- Оператору предложено остановить повторные попытки большой кнопкой. Позже
  оператор прямо подтвердил «ОСТАНОВИЛ»; остановка подтверждена его сообщением.
  Fixture содержит только тестовый endpoint;
  реального доступного VPN за ним не предполагается. Подключение не было частью теста.
- Следующий скриншот раздела «О приложении» подтвердил отображаемую версию
  **DefaultVPN 2.0.1.1 (Aug 22 2026, cb7ea0c)**. NAME_FAIL .conf привязан к ней.
  Это version evidence из UI, не проверка подписи/происхождения бинарника.
- Версия iOS, параметры сохранённого профиля, persistence после restart и поведение
  коллизий остаются UNKNOWN. Рабочих configs/keys/backup, повторного подключения
  или дополнительных сведений об iOS для текущего результата не требуется.

Второй скриншот пришёл под тем же локальным именем Без имени.jpg; его SHA256:
4d93c64315c61ac4bd934782973aa1546b73db5c11f4db992c195f1f2fbd267c.
Это другой файл по содержимому. Первый hash выше относится к экрану Server 1 /
Reconnecting…; второй — к версии приложения. Остановка не видна на втором экране;
она подтверждена последующим сообщением оператора «ОСТАНОВИЛ», а не этим изображением.

Предыдущий NAME_PASS через synthetic vpn:// от 16.09 сохраняется отдельно.
Для именования DefaultVPN предпочтителен уже проверенный native путь с description;
этот результат не обосновывает изменение всех .conf генераторов или Server 1 rename
как способ пройти NAME_PASS. Механизм получения имени и одинаковость двух builds
этим скриншотом не доказаны. Рабочий профиль «Испания» не предлагалось менять.
Серверы, package016, delivery handlers и общая выдача не изменялись;
письмо поддержке позднее отправлено оператором; статус и границы ниже.

### Следующая граница после NAME_FAIL и остановки — 2026-09-20

Оператор дополнительно ответил: просмотра параметров и экспорта отдельного
тестового профиля в интерфейсе нет. Parameter preservation остаётся UNKNOWN;
общий backup рабочих профилей, повторный import и подключение не запрашиваются.

Read-only source check: AMN2 checkout
C:/Users/SooL/Documents/amn2-phase15-local-package-bootstrap-readiness,
HEAD 56540e2084140e3a6277d7472c88c599d7153ccf, рабочее дерево чистое.

- app/vpn/client_import_artifacts.py: build_client_import_artifact уже создаёт
  native envelope с description=Neobyatnaya.NET для defaultvpn. Новая реализация
  генератора имени не требуется; compatibility_note не подтверждает acceptance.
- app/vpn/config_templates.py:97: без target_client build_vpn_import_link оставляет
  legacy Base64 от raw .conf; с target_client вызывает native exporter. Совпадение
  префикса vpn:// не делает эти payload одинаковыми.
- app/bot/delivery.py:88–141: build_config_delivery пока не принимает target_client;
  link создаётся в legacy режиме, attachment остаётся .conf, QR содержит raw config.
- app/services/config_delivery.py:96 и app/bot/workflows.py:766 используют этот
  legacy constructor. Поменять только подпись/расширение недостаточно.

Конкретная будущая интеграция после клиентской проверки: явный выбор клиента,
согласованные filename/content/link/encoding/caption из одного artifact, отдельное
решение для QR (его native compatibility не подтверждена), сохранение старого
режима и проверка обоих call sites. Сейчас delivery и источники AMN2 не менялись;
прежние 52 offline PASS не повторялись и не считаются проверкой iOS build.

Official lookup cb7ea0c в amnezia-vpn/DefaultVPN и amnezia-vpn/amnezia-client
вернул 422 No commit found. Точное соответствие установленной сборки публичному
исходнику не подтверждено; прежний dev source нельзя объявлять её реализацией.
Подготовлен [конкретный запрос разработчикам](phase16-defaultvpn-2.0.1.1-compatibility-question-draft-2026-09-20.md),
SENT_OPERATOR_REPORTED / WAITING_REPLY: правила имени .conf, источник сборки
и проверка одного профиля/MTU. Оператор сообщил «отправил»; доставка и ответ
независимо не проверены. DefaultVPN отложен как несрочный по его просьбе.
GitHub issues в официальном DefaultVPN отключены (has_issues=false); 44 записи
API /issues являются PR. PR #23 «Add dynamic server name from config» меняет
только заголовок страницы настроек, не импорт .conf. Адресат подготовленного
письма — support@dfvpn.com из раздела «О приложении»; ссылки и границы проверки
собраны в черновике. Агент не отправлял сообщения или рабочие secrets.
До ответа/другого проверяемого evidence не закрывать parameter gate и не включать
native delivery для реальных пользователей. Новая реальная выдача не выполнялась.

### Upstream readback — 2026-09-20 14:40 Europe/Moscow

GitHub API: #3043 OPEN, 14 comments; после сообщения оператора 08.09 новых нет.
#3113 OPEN / NOT MERGED, head a39f1c374ed760f886c62741d5645fd1c39c6630;
новых discussion comments после 12.09 нет. Новый полный обзор releases/reviews
этим readback не выполнялся. Оснований повторять закрытые Windows traffic
диагностики из этих данных нет. Сообщения upstream не отправлялись.
